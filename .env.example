# CRA (dev / build). Без завершающего `/`.
REACT_APP_API_BASE_URL=http://localhost:10902

# Рантайм: поле задаётся в public/runtime-config.js.
# В образе nginx скрипт docker/10-runtime-config.sh берёт API_BASE_URL (или REACT_APP_API_BASE_URL) из env при старте контейнера.
