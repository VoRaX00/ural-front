// Локально: пустой объект — используется REACT_APP_API_BASE_URL из сборки.
// В проде файл можно перезаписать при деплое (например, скриптом в docker-entrypoint.d).
window.__RUNTIME_CONFIG__ = window.__RUNTIME_CONFIG__ || {};
